// Primitive Data Types

const name = "silience"; //string
const age = 23; // number
const real = true; // boolean
const car = null; // shows object (due to a conventional error but in actual is primitive)
let test; // undefined
const sym = Symbol();

// Refrence Data Types

// Array

const hobbies = ["movies", "music"];

// object literals

const address = {
  state: "J & K",
  city: "srinagar",
};
const today = new Date();

console.log(new Date());
console.log(typeof address);